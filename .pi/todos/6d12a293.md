{
  "id": "6d12a293",
  "title": "Task 1: Add ChevronUpIcon and store methods for reordering",
  "tags": [
    "reorder-bookmarks"
  ],
  "status": "closed",
  "created_at": "2026-02-08T19:05:40.805Z"
}

Plan: .pi/plans/2026-02-08-reorder-bookmarks.md

## Task
1. Add `ChevronUpIcon` to `src/components/ui/Icons.tsx` (mirror ChevronDownIcon with path `m6 15 6-6 6 6`)
2. Export it from `src/components/ui/index.ts`
3. Add `moveFolder(id, direction: 'up' | 'down')` to bookmarks store — swaps adjacent items in folders array, persists
4. Add `moveTrade(folderId, tradeId, direction: 'up' | 'down')` to bookmarks store — swaps adjacent items in folder's trades array, persists

## Acceptance Criteria
- [ ] ChevronUpIcon renders correctly
- [ ] moveFolder swaps folder positions and persists to storage
- [ ] moveTrade swaps trade positions within a folder and persists to storage
- [ ] Both trigger syncService.schedulePush()
